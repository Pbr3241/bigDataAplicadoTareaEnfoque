from setuptools import setup, find_packages

setup(
    name="proyecto-bigdata",
    version="0.1",
    packages=find_packages(),  # Esto buscará todos los paquetes (carpetas con __init__.py) dentro del proyecto
)
